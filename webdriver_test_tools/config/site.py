
class SiteConfig(object):
    """Basic class for SiteConfig. Since this is specific to a project, this particular class isn't implemented,
    but exists to define an interface for the 2 variables that need to be declared in SiteConfig.

    :var SiteConfig.SITE_URL: URL of the home page
    :var SiteConfig.BASE_URL: Base URL for site pages (followed by a '/')
    """
    SITE_URL = ''
    BASE_URL = ''

